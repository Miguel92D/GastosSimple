# NAVIGATION RULES

1. Navigation only allowed in `/lib/core/flow/`
2. Widgets must not use `Navigator` directly
3. Controllers must not navigate
4. Services must not navigate

This prevents the project from becoming disorganized and centralizes the application flow logic.
